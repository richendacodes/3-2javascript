function concatenater() {
  var myName = document.getElementById("myName").value;
  var myShow = document.getElementById("myShow").value;
  var weekDay = document.getElementById("weekDay").value;

  alert(myName + " watches " + myShow + " on " + weekDay + "s!");

}